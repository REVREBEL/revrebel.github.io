/** @jsx jsx */
import { ComponentProps } from 'react';
import { jsx } from 'theme-ui';
import Card from './Card';
export interface TypeStyleProps extends ComponentProps<typeof Card> {
    fontSize?: number | string;
    fontFamily?: string;
    lineHeight?: string;
    fontWeight?: string;
    truncate?: boolean;
}
export declare const TypeStyle: ({ fontSize, fontFamily, lineHeight, fontWeight, children, truncate, ...props }: TypeStyleProps) => jsx.JSX.Element;
export default TypeStyle;
